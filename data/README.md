# Local task data

SQLite databases, task caches, thumbnails, embeddings, logs, and export transaction state are stored here. Generated contents are ignored by Git.

Source datasets are never placed here and remain read-only.

